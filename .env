# Local environment (gitignored). Copy .env.example -> .env and fill values.
# NOTE: sensitive keys were removed from the repository and moved to .env.example
# To run locally, create a .env file with the required values.
# Branding: FreshTrack (non-functional metadata)
